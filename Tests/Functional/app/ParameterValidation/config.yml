imports:
    - { resource: ../config/default.yml }

framework:
    serializer:
        enabled: true

exsyst_api:
    parameter:
        validation:
            enabled: true
            attribute_name: apiErrors
